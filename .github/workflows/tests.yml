name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unittest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Upgrade pip
        run: python3 -m pip install --upgrade pip
      - name: Install package with dev extras
        run: pip install -e ".[dev]"
      - name: Run unit tests
        run: python3 -m unittest discover -s tests -v
      - name: Verify CLI entry point
        run: |
          ai-divination tarot --spread three-card --seed demo > /tmp/tarot.json
          ai-divination iching --method yarrow --seed demo > /tmp/iching.json
          ai-divination xiaoliuren --method numbers --month 3 --day 12 --hour 7 > /tmp/xlr.json
          python3 - <<'PY'
          import json, pathlib
          for path in ["/tmp/tarot.json", "/tmp/iching.json", "/tmp/xlr.json"]:
              data = json.loads(pathlib.Path(path).read_text())
              assert isinstance(data, dict), path
          print("CLI smoke OK")
          PY
      - name: Build package
        if: matrix.python-version == '3.11'
        run: python3 -m build
      - name: Smoke test built wheel
        if: matrix.python-version == '3.11'
        run: |
          python3 -m venv /tmp/ai-divination-smoke
          /tmp/ai-divination-smoke/bin/python -m pip install dist/*.whl
          /tmp/ai-divination-smoke/bin/ai-divination tarot --deck major --spread single --seed ci
          /tmp/ai-divination-smoke/bin/ai-divination iching --method yarrow --seed ci
          /tmp/ai-divination-smoke/bin/ai-divination xiaoliuren --method numbers --month 3 --day 12 --hour 7
          /tmp/ai-divination-smoke/bin/ai-divination template tarot

---
description: "I Ching skill — call the local CLI, never invent the draw"
globs: ["**/*"]
alwaysApply: false
---

# Cursor adapter — I Ching

This skill lets the Cursor agent perform a I Ching reading.

## Hard rules
1. Never invent the cards / hexagram / position. Run the CLI:
   ```bash
   ai-divination iching --method yarrow
   ```
   (Omit `--seed` for real readings; seeded mode is for tests and reproducible demos only.)
2. Parse the JSON output. Interpret using `../SKILL.md` plus
   `../references/interpretation-template.md`.
3. Refuse medical / legal / financial / emergency / crisis advice — see
   `../../../shared/safety-policy.md`.

## When to trigger
Cast I Ching hexagrams for change analysis.

## Default example
> How should I approach the conversation with my co-founder this week?
